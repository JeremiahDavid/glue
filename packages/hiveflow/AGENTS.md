# hiveflow (CLI meta-package)

Thin CLI entrypoints (`src/hiveflow/cli.py`) that depend on the other packages.

Prefer working in the owning domain package instead of this one. Open the **repo root** only for full deploy / multi-package changes.
