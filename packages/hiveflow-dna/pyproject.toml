[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hiveflow-dna"
version = "0.1.0"
description = "HiveFlow DNA semantic engine (compile, validate, publish, governance)"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.35.0",
    "pyyaml>=6.0.0",
    "pyarrow>=18.0.0",
    "jsonschema>=4.23.0",
]

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true

[tool.setuptools.package-data]
"hiveflow.dna" = ["packs/*", "schema/*"]
"hiveflow.dna.source_docs" = ["schemas/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
